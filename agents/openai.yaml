interface:
  display_name: "ANC Transcribe Audio"
  short_description: "Turn local audio into one complete Markdown transcript"
  default_prompt: "Use $anc-transcribe-audio to create one complete Markdown transcript; I authorize uploading this specific audio to the ChatGPT transcription endpoint for that purpose."
